networks:
  tms: 
    driver: bridge
    
services:
  client:
    container_name: client
    build: ./client
    ports:
      - 3000:3000
      
  # controller:
  #   container_name: controller
  #   build: ./controller
  #   ports:
  #     - 3001:3001
    
    
